


        // let miArray : Array<number> = [1, 2, 5, 8];
        // let miVariable : Array<ComicDTO>;
        // miVariable = new Array<ComicDTO>();
        // miVariable.push(new ComicDTO());
        // miVariable.splice(0, 1); //elimina objetos de un array desde la posición inicial hasta el número de posiciones descritas
        
        // miVariable = [];  //para limpiar la lista del array 
        // let arrayObjetos = [new Date, 1, "dkjsgakalf", true, new ComicDTO];
      
        // let miMapa : Map<string, string>;
        // miMapa = new Map<string,string>();
        // miMapa.set("1", "semillero");  //llave 1 y valor semillero
        // miMapa.forEach((value : string, key : string ) => {
        //     let valorMapa = miMapa.get(key);
        // });
        
        // for (let llave; miMapa.keys;) {
        //     let valor = miMapa.get(llave)
        // }

        // miVariable.forEach(elemeno => {

        // })

        // let objeto = miVariable.map(elemento => {
        //     return {"nombre": elemento.nombre , "id": elemento.id}
        // })

        // for (let index = 0; index < miVariable.length; index++) {
        //     let elemento = miVariable[index];
        // }

        // let miFecha = new Date("2019-01-01");

          // private obtenerTipoDeDatoYValor(opcionRetorno : number) : void {
            
        //     let test = null;
            
        //     let resultado = test === null ? true : false;
        //         // o
        //     if (test !== null && test !== undefined && test.trim() !== "") {
        //         console.log("Mi variable test es nula");
        //     }else {
        //         console.log("Mi variable test NO es nula");
        //     }
        // }

        //let heightImagen = 100;